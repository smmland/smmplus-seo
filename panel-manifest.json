{
    "app": "smmplus-seo-panel",
    "version": "1.19.4",
    "notes": "Telegram Channel > Queue: the 'Post' column (title + message preview) was noticeably wider than it needed to be, crowding out the other columns - narrowed it down (both in the pending queue and the sent-history table below it) so the page reads more evenly. No functional changes, no database changes."
}
