{
    "app": "smmplus-seo-panel",
    "version": "1.27.19",
    "notes": "The public reviews API (GET /api/reviews - params: ?lang=, ?limit=) now automatically rotates which approved reviews it shows: the selection changes every 6 hours on its own (no cron/settings needed), staying stable for repeat requests within that window so a page that loads once renders consistently. The response includes a new 'rotates_at' timestamp so the frontend can know when the next change happens. No database changes."
}
