{
    "app": "smmplus-seo-panel",
    "version": "1.27.15",
    "notes": "Fixes an uncaught 500 error when two near-simultaneous requests from the same IP both trigger an auto-block at once (e.g. two rapid abusive requests, or a Tor exit node hit from two directions together) - the second attempt used to crash on a database duplicate-entry error instead of recording its block cleanly. It now recovers and still escalates the block correctly. No database changes."
}
