{
    "app": "smmplus-seo-panel",
    "version": "1.27.24",
    "notes": "Fixes a bug from the previous release: POST /api/reviews rejected a numeric user_id (e.g. {\"user_id\": 29}) with 'The user id field must be a string' - the site's own templating sends it as a raw JSON number on at least one page. user_id/order_id/ticket_id now accept either a number or a string. No database changes."
}
