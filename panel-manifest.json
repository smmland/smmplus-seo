{
    "app": "smmplus-seo-panel",
    "version": "1.31.0",
    "notes": "Adds automatic Telegram post view top-up. Every 15 minutes, recent public-channel posts below the configurable target (500 by default) receive only the missing quantity through the selected private upstream service. Includes delivery cooldown, lookback and per-run safety limits, visible counter/order/error history, and strict fixed-host public counter checks. Run Update database after installation."
}
