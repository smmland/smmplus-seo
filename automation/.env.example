# Shared secret the Laravel app must send as `Authorization: Bearer <token>` on every request.
# Generate with: openssl rand -hex 32
AUTOMATION_TOKEN=change-me

PORT=4790

# Optional: pin a specific Chromium build instead of Playwright's default resolution
# (already the case in the Claude Code sandbox via PLAYWRIGHT_BROWSERS_PATH).
# CHROMIUM_EXECUTABLE_PATH=
