{
    "app": "smmplus-seo-panel",
    "version": "1.16.1",
    "notes": "Telegram Queue's \"Generate now\" button showed a bare \"Nothing new to schedule / the week ahead is already fully scheduled\" with no way to tell why - now includes the actual numbers (how many posts are already scheduled vs. how many are wanted) so it's clear whether that's expected or worth investigating further. Also guarded topUpBlogPlan()/draftServiceChanges() against the telegram_posts table not existing yet (a database update not yet installed), so that fails with a clear message instead of a raw error."
}
