{
    "app": "smmplus-seo-panel",
    "version": "1.27.6",
    "notes": "Reworks 'Block all Tor exit nodes now' (Security Settings): instead of sending cPanel one block request per IP (even batched a few at a time), it now writes the whole exit-node list straight into cPanel's .htaccess in a single read + a single write - faster and far less likely to trip cPanel's own API rate limiting than thousands of individual requests. No database update needed for this update."
}
